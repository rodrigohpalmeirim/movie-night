<!--
	The group shell's error boundary: a page inside the club failed, so the card
	deals into the shell's own column — header and tab bar stay up, and the other
	tabs remain a one-tap way out even before "Try again" is considered.
-->
<script lang="ts">
	import ErrorCard from '$lib/components/ErrorCard.svelte';
</script>

<ErrorCard />
